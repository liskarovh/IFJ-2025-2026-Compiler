// Program 1: Vypocet faktorialu (iterativne, bez overeni celociselnosti)
import "ifj25" for Ifj
class Program {
    {
        static main() {
            var a 
            var b 
            b = ((3 - 2))
            a = 2 * (5 + 2 * 3) / 2
            {
                main()
                //return
            }
        }
    }

    static funkce(a, b) {
        if(a == 5) {
            a = 6
        } else {
            funkce(a, b)
            Ifj.write(a, b)
            a = 3
        }

        while(b < 2) {
            if(b == 0) {
                break
            } else {

            }
            
            b = b + 1
        }

        funkce(a, b)

        return a
    }

    static value {
        return __val
    }

    static value=(v) {
        __val = v
    }

    static f(a, b) {
        var a
        a = 5
        if(a == "a") {

        } else {
            a = Ifj.read() + b
        }
    }
}

class TvojeMama {
    
}
